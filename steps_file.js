// In this file you can append custom step methods to 'TM' object

module.exports = function () {
  return actor({
    // Define custom steps here, use 'this' to access default methods of TM
  });
};
